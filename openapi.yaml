openapi: 3.1.0
info:
  title: JIT Plugin – Passthrough Enabled
  version: "1.0"
servers:
  - url: https://gpt-dev-log-api.onrender.com
paths:
  /sheet/write_passthrough:
    post:
      operationId: writePassthrough
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - sheet_name
              properties:
                sheet_name:
                  type: string
              additionalProperties: true
      security:
        - BearerAuth: []
securitySchemes:
  BearerAuth:
    type: http
    scheme: bearer
